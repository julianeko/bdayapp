import React from "react";

function Main() {
//   const [name, setName] = useState([]);
//   let arr = name.map((element) => <Entries nname={name} nsetName={setName} />);
  return (
    <div>
      <ul>
        <li>
          Hier kommen ebenfalls die Namen und Daten hin, aber nur die, die im
          naechsten Monat Geburtstag haben
        </li>
      </ul>
    </div>
  );
}

export default Main;
